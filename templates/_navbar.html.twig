
    <nav class="navbar navbar-expand-lg navbar-secondary bg-primary">
        <a class="navbar-brand" href="{{ path('app_index') }}">
            <img class="logo" src="{{ asset('build/images/logocroped.jpg') }}" alt="logo"/>
        </a>
        <button class="navbar-toggler border-secondary" type="button" data-toggle="collapse"
                data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-item nav-link active text-secondary" href="{{ path('app_index') }}">{{ 'Home'|trans }}<span class="sr-only">(current)</span></a>
            </div>
            <div class="navbar-nav">
                <a class="nav-item nav-link active text-secondary" href="{{ path('program_index') }}">{{ 'Programs'|trans }}<span
                            class="sr-only">(current)</span></a>
            </div>
            <div class="navbar-nav">
                <a class="nav-item nav-link active text-secondary" href="{{ path('category_index') }}">{{ 'Categories'|trans }}<span
                            class="sr-only">(current)</span></a>
            </div>
            {% if app.user %}
                {% trans with {'%username%': app.user.email} %}
                    hello_username
                {% endtrans %} !
                <div class="navbar-nav">
                    <a class="nav-item nav-link active text-secondary" href="{{ path('app_profile') }}">{{ 'My profile'|trans }}</a>
                </div>
                <div class="navbar-nav">
                    <a class="nav-item nav-link active text-secondary"
                       href="{{ path('app_logout') }}"> {{ 'account.logout'|trans }}</a>
                </div>
            {% else %}
                <a class="nav-item nav-link active text-secondary"
                   href="{{ path('app_login') }}">{{ 'account.login'|trans }}</a>
            {% endif %}
            <div class="nav-item dropdown">
                <a class="nav-link dropdown-toggle bg-secondary" href="#" id="dropdown-language" role="button"
                   data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="far fa-flag fa-1.5x"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdown-language">
                    <a class="dropdown-item" href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({ '_locale': 'en' })) }}">English</a>
                    <a class="dropdown-item" href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({ '_locale': 'fr' })) }}">Français</a>
                </div>
            </div>
            <ul>
                {{ render(controller(
                    'App\\Controller\\DefaultController::navbarTop'
                )) }}
            </ul>
        </div>
    </nav>
