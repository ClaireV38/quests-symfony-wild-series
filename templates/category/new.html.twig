{# templates/category/new.html.twig #}

{% extends "base.html.twig" %}

{% block body %}
    <div class ="container">
        {{ form_start(form) }}
        {{ form_row(form.name, { 'label': 'nom de la categorie' }) }}
        <input type='submit' class="btn-submit" value='Add category'>
        {{ form_end(form) }}
    </div>
{% endblock %}