{# templates/program/new.html.twig #}

{% block title %}create a new program{% endblock %}

{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
        {{ form_row(form.title, { 'label': 'titre' }) }}
        {{ form_row(form.poster, { 'label': 'lien vers image poster' }) }}
        {{ form_row(form.category, { 'label': 'choisir le catégorie' }) }}
        {{ form_row(form.country, { 'label': 'choisir le pays' }) }}
        {{ form_row(form.year, {'label': 'année de diffusion' }) }}
        {{ form_row(form.synopsis, { 'label': 'synopsis' } ) }}
        {{ form_row(form.actors, { 'label': 'choisir les acteurs :' } ) }}
        <input type='submit' class="btn-submit" value='éditer la série'>
        {{ form_end(form) }}

    </div>
{% endblock %}
